spring:
  application:
    name: smart_city_management_system

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/your_database_name
    username: root
    password: root_1234 


  jpa:
    hibernate:
      ddl-auto: none # Change this to 'update' or 'create' if needed
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

server:
  port: 9090

logging:
  level:
    root: INFO